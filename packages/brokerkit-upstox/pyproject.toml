[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brokerkit-upstox"
version = "0.0.0"
dependencies = [
    "brokerkit-core",
    "upstox-python-sdk",
    "requests",
    "flask",
]
