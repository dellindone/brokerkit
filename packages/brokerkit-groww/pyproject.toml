[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brokerkit-groww"
version = "0.0.0"
dependencies = [
    "brokerkit-core",
    "growwapi",
    "pyotp",
]
