# BrokerKit Build Roadmap

Goal for Milestone 1: **Groww working end-to-end** — authenticate, look up instruments, place/track orders, read portfolio, fetch quotes, stream ticks — through the framework's own interfaces.

Layout convention: `packages/<hyphen-name>/` is the pip distribution (owns `pyproject.toml`); the folder inside it (`brokerkit` for core, `brokerkit_<broker>` elsewhere) is the importable Python package — hyphens are illegal in module names. Code always goes in the inner underscore folder. (Inner dirs of groww/fyers/upstox/paper/replay were renamed from hyphens to underscores on 2026-07-15.)

How to use this file: work through tasks in order. Each task lists the file(s) to create and what "done" means. Ask your guide by task number when stuck (e.g. "stuck on 2.3").

Decisions already made:
- Build one broker (Groww) end-to-end first; core grows only as the integration demands.
- Groww adapter wraps the official `growwapi` SDK (not raw REST) — it's the only way to get streaming, and auth becomes trivial.
- Auth: **TOTP-only** in v1 — `GrowwAuth(api_key, totp_secret)`, both required. (Groww's UI offers TOTP token or direct access token; the static-token mode was considered and dropped from v1 for simplicity — easy to add back as an optional `access_token` param. The old key+secret/approval flow is removed from Groww's UI — do not implement it.)
- SEBI compliance (per Groww dashboard banner): a registered **static IP** is mandatory for API trading (deadline was 31 Mar 2026). API calls may be rejected from unregistered IPs — register via "Add static IP" on the dashboard if requests fail.
- Instrument interface: **thin-adapter** (reshaped 2026-07-19, user decision): single method `fetch_instruments() -> list[Instrument]` — fetch + normalize + return, **no storage in the framework** (SDK's DataFrame copy flushed after normalize). Storage/lookups/search are app-level (→ M3.5 DB store). Dropped from the ABC same day: `search`, then `get_instrument`/`get_by_token`/`refresh` (whole cache layer). Consequence for Phase 6: feed subscribe takes `list[Instrument]` from the app (model carries the exchange/segment/token triple the feed topics need — verified against GrowwFeed._get_topics); framework maps ticks only for active subscriptions.
- **Async-native**: every interface method is `async def`. Adapters wrap sync vendor SDK calls with `asyncio.to_thread(...)`. Blocking calls must never run directly inside interface methods.
- **Core models use Pydantic v2** (`pydantic>=2` is a `brokerkit-core` dependency): validation at construction, JSON serialization for the FastAPI/AI layers. Enums stay stdlib `enum.Enum`/`StrEnum`.
- **Instance-scoped state only** (keeps multi-account possible later): no module-level tokens, clients, or caches. One provider/broker instance = one account. `BrokerManager` (built 2026-07-20, post-M1) composes N named broker instances on top of this — see Phase 7.5 below.

---

## Phase 1 — Auth

- [x] **1.1** `packages/brokerkit-groww/pyproject.toml` — add `growwapi` and `pyotp` deps (this package only). Install them in `.venv` and poke `GrowwAPI.get_access_token()` in a REPL to see what it returns (bare string vs object with expiry).
- [x] **1.2** `brokerkit-core/brokerkit/models/auth.py` — `AuthToken` model: token string, expiry datetime (Groww tokens die 6:00 AM IST), helper like `is_expired`.
- [x] **1.3** `brokerkit-core/brokerkit/exceptions/common.py` + `exceptions/auth.py` — a `BrokerKitError` base, then `AuthenticationError`, `TokenExpiredError`.
- [x] **1.4** `brokerkit-core/brokerkit/interfaces/auth.py` — `AuthProvider` ABC: `login() -> AuthToken`, `get_token() -> AuthToken`. (`can_refresh` removed 2026-07-15 — YAGNI while every provider is TOTP/refreshable; reintroduce if a non-refreshable auth mode appears.)
- [x] **1.5** `packages/brokerkit-groww/.../auth.py` — `GrowwAuth` (TOTP-only), verified with real credentials 2026-07-15. Leftover cleanups: remove dead `if not self.totp_secret` branch, add `can_refresh` property, split `import asyncio, pyotp`. Revisit after 1.4 to make it implement `AuthProvider` and return `AuthToken` instead of `str`.

## Phase 2 — Instruments

> Phase 2 complete 2026-07-19. (Root cause of the earlier "unsaved" re-export: top-level `__init__.py` content had been saved into `models/__init__.py`. Fixed + committed e849dc4.)

- [x] **2.1** `enums/exchange.py` + `enums/segment.py` — `Exchange`/`Segment` as **StrEnum** (NSE/BSE/MCX; CASH/FNO/COMMODITY/CURRENCY). Done, verified.
- [x] **2.2** `models/instrument.py` — `Instrument` (pydantic, Decimal tick_size; derivative fields expiry/strike/underlying dropped for v1 — YAGNI, re-add as optional when F&O comes). Done.
- [x] **2.3** `interfaces/instrument.py` — `InstrumentProvider` ABC (get_instrument, get_by_token(exchange_token, exchange, segment) — raises not returns None, search, refresh) + `exceptions/instrument.py` with `InstrumentNotFoundError`. Done, incl. top-level re-export (e849dc4).
- [x] **2.4** `packages/brokerkit-groww/.../instruments.py` — `GrowwInstruments(InstrumentProvider)` sourcing data via the SDK: `to_thread(client.get_all_instruments)` (downloads the public CSV as a str-dtype DataFrame), build own (symbol|token, exchange, segment)→Instrument dicts in one pass, skip unknown-enum rows. SDK's own lookup methods unused (no segment filter/search/refresh); `refresh()` = `client.instruments = None` + reload. SDK writes `instruments.csv` to cwd's parent — gitignore it. Done when `get_instrument("RELIANCE", NSE, CASH)` returns a populated model. Verified live 2026-07-19 (dummy token works — CSV is public; 145,746 instruments cached).
- [x] **2.5** `packages/brokerkit-groww/.../broker.py` — minimal `GrowwBroker` (pulled forward from 7.1 so framework users never import `growwapi`): `__init__(totp_key, totp_secret)` builds `GrowwAuth` internally (TOTP-only decision reaffirmed 2026-07-19 — no auth injection, no static-token mode); built as an async `create()` classmethod factory (user's improvement over the planned `connect()` — no half-constructed broker can exist): gets token, builds the one shared `GrowwAPI` client, wires `self.instruments` (+ `self.orders` since 3.7). Done: `await GrowwBroker.create(k, s)` works with zero growwapi imports in user code.

## Phase 3 — Orders

> Milestone 1 is **code-complete** (Phases 1–7). Remaining for full M1 sign-off: the two dashboard actions below, then run `examples/basic/main.py` + `scratch_orders.py` live. After that, pick the next milestone with the user (M2 middleware / M3 testing / M3.5 instrument store / M4 paper broker). **Two pending user actions on the Groww dashboard:** (1) register static IP ("Add static IP") — unblocks 3.7 live order test; (2) subscribe to the paid live-data API plan (₹499/mo + taxes) — unblocks Phase 5 live test (quote/LTP/OHLC/candles return "Access forbidden" without it; verified via raw SDK, not a framework bug) and Phase 6 streaming (live ticks also need market hours). Verified live so far (2026-07-19): auth, instruments, portfolio holdings, error translation (IP rejection arrived as core `OrderError`). Test scripts at repo root, uncommitted: `scratch_orders.py`, `scratch_market.py`.

- [x] **3.0** F&O/options support in core (user decision 2026-07-19 — options trading wanted, incl. for future AI-agent consumers who need typed fields, not symbol parsing): `enums/instrument_type.py` (`InstrumentType`: EQ/FUT/CE/PE/IDX — exact CSV values, verified); `Instrument` gains optional `expiry: date`, `strike: Decimal`, `underlying: str` (None for equities) and `instrument_type` becomes the enum; Groww normalization maps expiry_date/strike_price/underlying_symbol. (Amends the 2.2 YAGNI drop — "re-add when F&O comes"; it came.)
- [x] **3.1** `brokerkit-core/brokerkit/enums/` — `order_type.py` (MARKET, LIMIT, SL, SL_M), `transaction_type.py` (BUY, SELL), `product.py` (CNC, MIS, NRML), `validity.py` (DAY, IOC), `order_status.py` (canonical 6: PENDING/OPEN/EXECUTED/CANCELLED/REJECTED/FAILED — Groww's 12 raw states collapse in the mapper). Values verified against SDK constants + docs annexure 2026-07-19; SDK extras deliberately excluded (BO/CO/MTF/ARB products, GTC/GTD/EOS validity, GTT/OCO smart orders) — unverified promises; add only when implemented+tested.
- [x] **3.2** `models/order.py` — `OrderRequest` (carries full `Instrument` object — design (b), gives mapper the symbol/exchange/segment triple free) + `Order` (flat trading_symbol/exchange/segment since it's built from broker responses). Decimal prices; `model_validator` enforces LIMIT/SL→price, SL/SL_M→trigger_price, MARKET→neither.
- [x] **3.3** `exceptions/order.py` — `OrderError` → `OrderRejectedError` → `InsufficientMarginError` (margin = a kind of rejection, so `except OrderRejectedError` catches both).
- [x] **3.4** `interfaces/order.py` — `OrderProvider` ABC. `segment: Segment` param on modify/cancel/get_order — Groww SDK requires it; core enum, so not a leak. (Method renamed `place`→`place_order` outside a Claude turn — kept consistent in ABC + Groww adapter.)
- [x] **3.5** `brokerkit_groww/mapper.py` — status map: 12 annexure statuses + `OPEN` (doc examples only) → canonical 6; CANCELLATION/MODIFICATION_REQUESTED→OPEN, COMPLETED/DELIVERY_AWAITED→EXECUTED; unknown status raises (loud > silent FAILED). `place_response_to_order` echoes request fields (place response is thin). `amo_status` deliberately not mapped (Groww-specific; escape hatch if ever needed: optional `raw: dict` on Order). Enum values identity-map (3.1 verified them against SDK strings).
- [x] **3.6** `brokerkit_groww/errors.py` — `groww_errors(default)` contextmanager; auth/instrument exceptions map explicitly, rest go to caller-supplied `default` (context decides domain). **Verified live**: unregistered-IP rejection surfaced as core `OrderError`.
- [x] **3.7** `brokerkit_groww/order.py` (note: singular filename, not orders.py) — `GrowwOrderProvider`, wired in `GrowwBroker.create()`. SDK quirk: `modify_order` requires quantity+order_type, so `modify()` fetches current order to fill unspecified fields; modify/cancel re-fetch via `get_order()` (thin SDK responses; correctness > latency). Decision taken: **no pre-validation** — let broker reject (freeze_quantity etc. stay dropped; revisit with M2 middleware). Code complete; **live place+cancel still unverified** — blocked on static-IP registration (see NEXT SESSION note). **Bug found + fixed 2026-07-20** while writing the Groww README: `place_order()` called `place_response_to_order(resp)` with only 1 arg instead of `(resp, request)` — a `TypeError` that was masked by the IP-registration block (crash never reached, since the SDK call itself fails first). Will only surface once a real order placement succeeds, so re-verify this path once static IP is registered.

## Phase 4 — Portfolio

> Margin dropped from v1 (user call 2026-07-19: "worth nahi hai") — no `Margin` model, no `margins()` on the ABC. Re-add as optional method + model if a strategy actually needs pre-trade margin checks (Groww's `get_available_margin_details` is there when wanted).

- [x] **4.1** `models/position.py` (net qty, buy/sell qty+avg from debit/credit, realised_pnl) + `models/portfolio.py` (`Holding`: demat-level, ISIN-identified, **no exchange field** — deliberate; pledged/t1 quantities kept).
- [x] **4.2** `interfaces/portfolio.py` — `PortfolioProvider` ABC: `holdings()`, `positions()` only.
- [x] **4.3** `brokerkit_groww/portfolio.py` — `GrowwPortfolio` + mapper `groww_to_holding`/`groww_to_position` (debit=buy, credit=sell), wired in `create()`. **Verified live 2026-07-19** (`holdings()` returned real data — so portfolio reads are NOT blocked by the missing static-IP registration; only order placement is).

## Phase 5 — Market Data (REST)

> Code complete + smoke-tested 2026-07-19 (imports, mapper unit checks, ABC contracts). **Live test blocked: Groww returns "Access forbidden" on data endpoints — needs the paid live-data API subscription on the dashboard** (auth/IP are fine — portfolio reads work). Phase 6 streaming will need the same subscription.

- [x] **5.1** `models/quote.py` (`Quote` + `Ohlc` + `DepthLevel`; offer→ask rename; 52wk/mcap/IV skipped — YAGNI; `open_interest` kept for F&O) + `models/candle.py`.
- [x] **5.2** `interfaces/market.py` (`MarketDataProvider`: get_quote single, get_ltp/get_ohlc batch — take `list[Instrument]`, return dicts keyed by `instrument.symbol`) + `interfaces/historical.py` (`HistoricalDataProvider.get_candles`, interval in minutes).
- [x] **5.3** `brokerkit_groww/market.py` (`_fetch_batched`: SDK takes ONE segment per ltp/ohlc call + max 50 symbols — groups by segment, chunks by 50, maps "NSE_SYMBOL" keys back) + `historical.py` (uses deprecated `get_historical_candle_data` — the V2 method needs `groww_symbol` which core `Instrument` doesn't carry) + mapper quote/candle/ohlc functions (`_epoch_dt` handles ms-vs-s ambiguity). Wired in `create()`.

## Phase 6 — Streaming

> Code complete 2026-07-19 (written by Claude at user's request — mode shift from guide-only). Smoke-tested: ABC contract, tick mapping, thread→loop dispatch, unknown-key drop. **Live tick test pending: needs the paid data subscription + market hours.**

- [x] **6.1** `interfaces/streaming.py` — `StreamingProvider` ABC: `subscribe_ltp(list[Instrument], callback)`, `unsubscribe_ltp`, `close()`. `TickCallback` accepts sync or async callables. + `models/tick.py` (`Tick`: symbol/exchange/segment/ltp/timestamp/volume/open_interest). LTP-only for v1 — GrowwFeed also offers market-depth/index/order-update feeds; add interface methods when needed.
- [x] **6.2** `exceptions/streaming.py` — `StreamingError` → `StreamingConnectionError`, `NotSubscribedError`; mapped in errors.py `_MAP` from `GrowwFeedConnectionException`/`GrowwFeedNotSubscribedException`.
- [x] **6.3** `brokerkit_groww/streaming.py` — `GrowwStreaming` wraps `GrowwFeed` (verified against SDK source: feed dicts need exchange/segment/exchange_token; callback fires on a NATS **thread** with meta only). Design: lazy feed construction on first subscribe (socket opens there); `(exchange, segment, token)` registry maps ticks back to Instruments — unknown keys dropped (roadmap decision: only active subscriptions map); NATS-thread callback bounces to the event loop via `call_soon_threadsafe`, then pulls the snapshot from `feed.get_ltp()` and builds `Tick` (proto field names: ltp/tsInMillis/volume/openInterest); async callbacks scheduled with `ensure_future`. Instruments without `exchange_token` are rejected at subscribe. Wired in `create()`.

## Phase 7 — Broker Assembly

> Complete 2026-07-20 (written by Claude, same mode as Phase 6). Smoke-tested: plugin discovery without pre-import, registry, unknown-broker error, ABC guard, example parses.

- [x] **7.1** `GrowwBroker` — was already done via `create()` (2.5/onwards); now `class GrowwBroker(Broker)` with `name = "groww"`.
- [x] **7.2** `brokerkit-core/brokerkit/assembly/broker.py` (abstract `Broker`: provider attributes + abstract `async create(**config)` + concrete `close()` that closes streaming; `__init_subclass__` auto-registers any subclass that sets `name`) + `registry.py` (name→class dict, clear error listing registered names) + `factory.py` (`create_broker(name, **config)`; unknown name → lazily imports `brokerkit_<name>` so adapters self-register on import — plugin pattern, core never depends on adapters). Same folder also has `registry.py` and `factory.py`. Top-level exports: `Broker`, `create_broker`. (2026-07-20: this file + registry.py/factory.py/broker_manager.py moved from `brokerkit/` root into `brokerkit/assembly/` for consistency with the enums/exceptions/interfaces/models folder convention — user's request.)
- [x] **7.3** `examples/basic/main.py` — create_broker → instrument lookup → quote (try/except: data subscription may be missing) → holdings → close. **Milestone 1 code-complete.** Live E2E run of the example still pending the two dashboard actions (static IP + data subscription).
- [x] **7.5** `brokerkit-core/brokerkit/assembly/broker_manager.py` — `BrokerManager` (user-requested, built by Claude 2026-07-20): named multi-account collection on top of the instance-scoped-state design decision. `add(account_id, broker_name, **config)` → `create_broker` internally; `get`/`__getitem__`/`__iter__`/`__len__`; `remove`/`close_all` call `broker.close()`. Duplicate account_id and missing-account both raise `BrokerKitError`. Smoke-tested with a fake in-process `Broker` subclass (no real creds needed) — add/get/iterate/duplicate-guard/missing-guard/remove/close_all all verified. Exported top-level.

## Post-M1 hardening (done ahead of M2)

- [x] **Token auto-refresh** (2026-07-20, user-prioritized after a "what am I not seeing" review): `GrowwBroker._auto_refresh_loop()` — background `asyncio.Task` started in `create()`, cancelled in `close()`. Sleeps until the token's known 6 AM IST expiry (no per-call polling needed — deterministic schedule), then `auth.login()` + mutates `self._client.token` in place. Works because `growwapi`'s `GrowwAPI.token` is a plain attribute re-read fresh on every request (verified against SDK source) — so every provider (orders/portfolio/market/historical/streaming), which all share the one `_client` instance, picks up the refreshed token automatically with zero changes to those files. Failures (network blip during refresh) are caught and retried after 60s rather than silently killing the background task. Functionally tested with a fake auth/client pair (short fake expiry) — 3 refresh cycles observed, client.token mutated correctly each time, task cancels cleanly on `close()`.
- [ ] **Order idempotency** — flagged in the same review, deliberately deferred. Groww's `place_order` accepts an `order_reference_id` for dedup; currently unused, so a retried `place_order()` call (e.g. after a timeout where the order actually went through) can double-place. Should land before this framework is trusted with real retry logic.

## Later milestones (coarse — will be broken down when we get there)

- **M2 Middleware**: rate limiting (Groww: orders 10/s 250/min, live data 10/s 300/min, other 20/s 500/min), retry, logging — `brokerkit-core/brokerkit/middleware/`. (Auth-refresh, originally planned here, was pulled forward and done above.)
- **M3 Testing package** (user decision 2026-07-20: deliberately last, not per-broker — write once, across all brokers together, once M5's second broker exists to prove the contract tests actually generalize): mocks/fixtures/contract tests in `brokerkit-testing`; contract test suite any adapter must pass.
- **M3.5 Instrument store (user idea 2026-07-19)**: DB-backed `InstrumentProvider` (SQLite first) — daily refresh job pulls each broker's master, normalizes to core `Instrument`, upserts; providers query on demand instead of holding ~500 MB in RAM. Schema keyed (broker, exchange, segment, symbol) with per-broker token columns; ISIN links the same equity across brokers. Same ABC, swap-in replacement — strategy code untouched. Build when the second broker or the data pipeline arrives.
- **M4 Paper broker**: simulated execution reusing Groww instrument data.
- **M6 Replay, News, AI packages.**

## Phase 8 — Fyers adapter (M5)

> **Verified live 2026-07-20:** `create_broker("fyers", ...)` end-to-end against a real account — TOTP+PIN auto-login (`FyersAuth`), `fetch_instruments()`, and `market.get_quote()` all confirmed working (via a sandbox script exercising the exact quick-start flow in the README). One-time manual step needed before this works for a *new* Fyers app: run `login_helper.py` once to activate the app via the official browser auth-code flow (unclear exactly why this is required — TOTP-based login alone returned "invalid totp" on a freshly-created, never-browser-authenticated app; worked immediately after one `login_helper.py` run). **▶ NEXT SESSION STARTS HERE:** orders/portfolio/historical/streaming are still only unit-checked against captured response shapes, not live-exercised — extend the sandbox test the way Groww's `scratch_orders.py` did for M1 (place/modify/cancel, holdings/positions, candles, a live tick subscription).

Locked in 2026-07-20 — not an arbitrary pick: Fyers is the user's primary *data* source since its market/historical/streaming data is free, unlike Groww's paid subscription; real usage plan in `brokerkit-broker-strategy` memory, not just abstraction-proving. Full `Broker` contract built (auth/orders/portfolio too — no data-only shortcut), with `MarketDataProvider`/`HistoricalDataProvider`/`StreamingProvider` getting the most scrutiny since that's the actual reason this adapter exists.

- [x] **8.1 Research** — official `fyers-apiv3` SDK (installed + read in full: `fyersModel.py`, `FyersWebsocket/data_ws.py`, shipped `map.json`), the official `FyersDev/fyers-api-sample-code` repo (cloned, request/response shapes cross-checked), rendered docs + a third-party typed Go SDK (cross-verification for response field names), and live-downloaded symbol-master CSVs across `NSE_CM`/`NSE_FO`/`BSE_CM`/`BSE_FO`/`MCX_COM` (column semantics aren't reliably documented anywhere — verified via real rows + the SDK's own `exch_seg_dict` fyToken-prefix convention, since a Fyers community post flags the "official" CSV header as wrong on its trailing columns).
- [x] **8.2 Auth** (`brokerkit_fyers/auth.py` — `FyersAuth`) — decision **superseded same day (2026-07-20)**: originally built as official browser-bootstrap + `/validate-refresh-token`+PIN (needed a one-time manual `login_helper.py` run). User then provided their own previously-working implementation of the unofficial TOTP+PIN auto-login (hits Fyers' internal login endpoints, `api-t2.fyers.in/vagator/v2/*` — the same pattern flagged and declined earlier, revisited once real working code existed to verify against, not a secondhand community script). Rebuilt `FyersAuth` around it: `login()` drives OTP→TOTP-verify→PIN-verify→internal-token-exchange, then finishes with the *official* documented step (`SessionModel.generate_token()`); no persisted refresh_token, `login()` just re-runs fresh each time — matches `GrowwAuth`'s ergonomics exactly (pure credentials in, zero manual steps). `login_helper.py` removed (superseded). `FyersBroker._auto_refresh_loop()` reshaped to Groww's sleep-until-assumed-expiry pattern instead of a fixed interval. Trade-off carried over and now more prominent: the auth_code-acquisition part isn't in any SDK/public API contract, could break on a Fyers-side UI/security change without notice — isolated entirely to this one file. Same `FyersModel.header`-caching gotcha as before still applies (mutate both `.token` and `.header` on refresh).
- [x] **8.3 Instruments** (`brokerkit_fyers/instruments.py` — `FyersInstruments`) — Fyers has no single combined instrument file like Groww; fetches 5 public per-exchange/segment CSVs concurrently (`asyncio.gather` + `to_thread`) and concatenates. Currency derivatives (`NSE_CD`) deliberately excluded — core's `Segment` enum has no `CURRENCY` value. Instrument type derived from the trading-symbol suffix (`-EQ`/`-INDEX`/`FUT`/`CE`/`PE`), not the CSV's numeric type column (verified inconsistent across files, no clean enum mapping). **Verified live** 2026-07-20, no auth needed.
- [x] **8.4 Orders** (`brokerkit_fyers/order.py`, `mapper.py`, `errors.py`) — big SDK-behavior difference from Groww: `fyers-apiv3` never raises on an API error, it returns `{"s": "error", ...}` dicts even for auth failures (`errors.py`'s `check()` inspects instead of catching). No single-order-lookup endpoint — `get_order`/`list_orders` fetch the whole orderbook and filter, same as the SDK's own `get_orders()` helper does internally. `modify()` sends only the changed fields (no pre-fetch needed, unlike Groww's SDK quirk). Status enum has a genuine semantic trap: Fyers' own "Pending"(6) means *live/working at exchange* (→ core `OPEN`), while "Transit"(4) means *not yet confirmed by the exchange* (→ core `PENDING`) — the reverse of what the names suggest.
- [x] **8.5 Portfolio** (`brokerkit_fyers/portfolio.py`) — `holdings()`/`positions()`, field names cross-verified against the SDK's own shipped `map.json` (used internally for the order-websocket) as an independent check. Fyers holdings carry no ISIN — `Holding.isin` always `None` for this adapter.
- [x] **8.6 Market Data + Historical** (`brokerkit_fyers/market.py`, `historical.py`) — highest-care files per the adapter's actual purpose. `/quotes` batches up to 50 symbols/call (chunked, like Groww); note it has no depth/circuit-limit fields (those live on a separate `/depth` endpoint not called here), so `Quote` is narrower than Groww's — a real broker capability difference, not a bug. `/history` resolution param uses `"D"` for daily (verified against the *official sample code*, which diverges from its own docstring's `"Day"`/`"1D"` claim).
- [x] **8.7 Streaming** (`brokerkit_fyers/streaming.py`) — thread+callback SDK (`FyersDataSocket`) bridged to the event loop the same way `GrowwStreaming` bridges `GrowwFeed`. Two SDK quirks found by reading source, neither present in the Groww adapter: (1) `FyersDataSocket` is a **process-wide singleton** — a second `FyersStreaming` (e.g. second account via `BrokerManager`) would silently hijack the first one's connection; detected and raised as `StreamingError` instead. (2) reconnects **wipe the SDK's own subscription bookkeeping** — `FyersStreaming` hooks the reconnect callback to replay tracked subscriptions automatically, untested against a real dropped connection though. `Tick.open_interest` is always `None` — confirmed the SDK itself strips that field from the LTP feed before dispatch, not an adapter gap.
- [x] **8.8 Broker assembly** (`brokerkit_fyers/broker.py` — `FyersBroker`, `name = "fyers"`) — same `create()` factory shape as `GrowwBroker`; `is_async=False` chosen deliberately over the SDK's own `is_async=True` mode for consistency with the rest of the framework (and because `FyersServiceAsync`'s exception-handling paths looked less robust than the sync ones when reading source).

## Phase 9 — Option chain + Greeks (core, Groww, Fyers)

> User-requested 2026-07-20 ("optionchain aur greeks bhi daal do groww aur fyers mein"). Core interface change (`MarketDataProvider.get_option_chain`), so both adapters had to move together — first cross-broker addition to `MarketDataProvider` since Phase 5.

- [x] **9.1 Research** — `growwapi.GrowwAPI.get_greeks`/`get_option_chain` found by inspecting the installed SDK's public methods (not in Groww's own docs page's field-name detail — response schema page lists fields but not the nesting); resolved via a third-party typed Go SDK (`rctrj/growwapi-go`) showing `strikes` is a `map[string]OptionChainStrike` (keyed by strike-price string), `ce`/`pe` lowercase, `greeks` nested with `rho` included. Fyers' `optionchain()` request shape was already known from Phase 8 research but the response had never been captured — **live-tested directly** (`{"symbol":"NSE:NIFTY50-INDEX","strikecount":2,"timestamp":"","greeks":"1"}`) since Fyers has no paid-data blocker: confirmed `optionsChain` is a flat array (first entry = the underlying itself, `option_type=""`, `strike_price=-1`), greeks nested under `"greeks"` with **no `rho`** (unlike Groww's). Also live-decoded real `expiryData` epoch values to confirm Fyers' `timestamp` param means 15:30:00 IST on the expiry date, not midnight — computed directly instead of guessed.
- [x] **9.2 Core models** (`brokerkit-core/brokerkit/models/option_chain.py`) — `OptionGreeks` (delta/gamma/theta/vega/iv, `rho: float | None` since Fyers doesn't have it), `OptionContract` (symbol/strike/option_type/ltp/open_interest/volume/greeks), `OptionChainStrike` (strike + optional call/put), `OptionChain` (underlying_symbol/underlying_ltp/expiry/strikes). Re-exported top-level.
- [x] **9.3 Interface** (`brokerkit-core/brokerkit/interfaces/market.py`) — `MarketDataProvider.get_option_chain(underlying: Instrument, expiry: date, strike_count: int = 10) -> OptionChain`, added as abstract (required on every adapter, like the rest of the ABC). `expiry` is mandatory — no "nearest expiry" convenience in v1, matches `get_candles`'s explicit-range precedent. `strike_count` is advisory only — Groww's endpoint has no such filter and always returns everything.
- [x] **9.4 Fyers implementation** (`market.py`, `mapper.py`) — **verified live** against a real account: real NIFTY strikes/greeks (delta 0.8→0.2 across strikes, coherent with spot ~24238), underlying_ltp pulled from the option_type="" entry, CE/PE grouped by `strike_price` into `OptionChainStrike`.
- [x] **9.5 Groww implementation** (`market.py`, `mapper.py`) — unit-tested against synthetic data matching the Go-SDK-verified structure (dict-of-strikes, nested greeks incl. rho); **not live-tested** — blocked by the same paid live-data subscription blocking `get_quote` etc. (Phase 5's known blocker, unchanged).
- [x] **9.6 bid/ask on OptionContract** (2026-07-20, same-day follow-up — user wants liquidity visible before trading options, sensible: wide spreads = bad execution). Fyers has `bid`/`ask` per contract — **verified live** (real spreads, e.g. 139.5/140.45). Groww does not — confirmed absent via *two* independent sources (a third-party typed Go SDK, and the official docs' field list, re-checked specifically for this), not guessed; `OptionContract.bid_price`/`ask_price` stay at their `None` default for every Groww contract, with a comment explaining why.

## Phase 10 — Upstox adapter (M?)

> Design locked 2026-07-20 (brainstorming session, both architecture forks resolved by user). Per [[brokerkit-broker-strategy]]: Upstox is earmarked for **fundamentals + news**, not execution — but same no-shortcuts rule as Fyers applies: full `Broker` contract gets built, with the fundamentals/news providers getting the most care since that's the actual reason this adapter exists. User's explicit call after seeing the design: implement everything decided now, test it, then diff "what Upstox's API actually offers" against "what we built" to decide on anything further — deliberately deferring further scope expansion past this phase.

- [x] **10.1 Research** — official `upstox-python-sdk` (PyPI; installed name differs from repo name `upstox-python`) installed + source read directly (`upstox_client/api/*.py` for every module used below — not just docs, since e.g. the SDK's actual method list is the only reliable way to know the v3/legacy split). Cross-checked against rendered docs (`upstox.com/developer/api-documentation/*`) and live-tested the public instrument-master URL (`curl` with a UA header — 200 + working range-request; a community report of 403 did not reproduce, likely a missing-UA or region issue on their end, not a real blocker).
  - **Auth**: OAuth2 authorization-code flow (`/v2/login/authorization/dialog` → `/v2/login/authorization/token`, exchanged via `LoginApi.token()` — form-encoded, `code/client_id/client_secret/redirect_uri/grant_type`, returns `TokenResponse` with `access_token` + `extended_token`, no expiry field — same "assume expiry" situation as Fyers). Separately, a **Analytics Token** exists: dashboard-generated only (no API), 1-year validity, read-only, no static IP needed, and — confirmed via the official Analytics Token doc page — explicitly covers Market Quote/Historical/Option Chain/**Fundamentals**/**News** (not Orders/Portfolio, which need the daily OAuth token + static IP for account-scoped calls).
  - **Instruments**: public gzipped JSON per-exchange or combined (`https://assets.upstox.com/market-quote/instruments/exchange/complete.json.gz`), no auth needed — live-verified reachable.
  - **Orders**: real SDK-side split — writes (`place_order`/`modify_order`/`cancel_order`) live in `OrderControllerV3Api` (no `api_version` param); reads (`get_order_book`/`get_order_details`/`get_trade_history`) live in the legacy-named `OrderApi` and require an explicit `api_version` header param on every call. GTT orders exist as a separate concern (`place_gtt_order` etc.) — out of scope for v1, same YAGNI precedent as Groww/Fyers' excluded order types.
  - **Portfolio**: `PortfolioApi.get_holdings`/`get_positions` (also `api_version`-required).
  - **Market data**: `MarketQuoteV3Api.get_ltp`/`get_market_quote_ohlc`/`get_market_quote_option_greek` — batches up to **500 instrument keys/call** (vs Groww/Fyers' 50).
  - **Historical**: `HistoryV3Api.get_historical_candle_data(instrument_key, unit, interval, to_date)` (+ an overload with `from_date`) and `get_intra_day_candle_data`.
  - **Option chain**: `OptionsApi.get_put_call_option_chain(instrument_key, expiry_date)` + `get_option_contracts`; greeks via `get_market_quote_option_greek`.
  - **Streaming**: `MarketDataStreamerV3` — event-callback SDK class (`.on("open"/"message")`, `.subscribe(keys, mode)`, `.change_mode()`, `.unsubscribe()`); protobuf decoding handled internally by the SDK (unlike Groww/Fyers, no manual byte-parsing needed). Two modes: `ltpc` (last-trade only, up to 5000 keys) and `full` (adds depth + intraday candles).
  - **Fundamentals**: confirmed live in SDK source (`upstox_client/api/fundamentals_api.py`) — 8 methods, all ISIN-keyed path params (`/v2/fundamentals/{isin}/...`): `get_company_profile`, `get_balance_sheet`, `get_cash_flow`, `get_income_statement`, `get_share_holdings`, `get_key_ratios`, `get_corporate_actions`, `get_competitors`. `type` (consolidated/standalone) and `fs` (full line-item breakdown) are optional params on the financial-statement ones.
  - **News**: confirmed live in SDK source (`upstox_client/api/news_api.py`) — `GET /v2/news`, `category` required (`instrument_keys`/`positions`/`holdings`), `instrument_keys` required only for that category (max 30, comma-separated), paginated (`page_number`/`page_size`). `positions`/`holdings` are resolved server-side against the authenticated account — no client-side instrument list needed, so no cross-provider dependency on `PortfolioProvider`. Returns last 7 days of articles only, keyed by instrument in the response.
- [x] **10.2 Auth** (`brokerkit_upstox/auth.py` — `UpstoxAnalyticsAuth`, `UpstoxOAuth`) — dual-credential design as planned: `UpstoxBroker.create()` accepts `analytics_token` and/or `client_id`+`client_secret`+`redirect_uri` independently. `UpstoxAnalyticsAuth` just wraps the static token (no network call, no expiry logic beyond a generous 360-day assumption). `UpstoxOAuth` reuses Fyers' `login_helper.py` Flask-capture pattern directly inside `login()` (no separate one-time helper file needed here — captures Upstox's `code` param the same no-copy-paste way), then exchanges via `LoginApi.token(api_version="2.0", ...)`. **Design changed from the original plan during implementation**: no background refresh task — since Upstox's OAuth token cannot be refreshed headlessly at all (no documented programmatic login, unlike Groww/Fyers), a proactive "sleep until expiry" loop would just fail unattended. Instead each OAuth-backed provider calls `AuthProvider.get_token()` (inherited lazy check-then-login) immediately before every SDK call, mutating the shared `Configuration.access_token` — confirmed from `configuration.py` source that Upstox's SDK (like Groww's, unlike Fyers') re-reads `access_token` fresh per call via `auth_settings()`, so this is safe. If only `analytics_token` is given, `orders`/`portfolio` stay unset (`AttributeError` on access). Data-facing providers prefer `analytics_token` when both are given.
- [x] **10.3 Instruments** (`brokerkit_upstox/instruments.py` — `UpstoxInstruments`) — fetches 3 public gzipped JSON files (`NSE`/`BSE`/`MCX`, not the single `complete.json.gz`) concurrently. **Live-verified 2026-07-20**: 113,048 real instruments parsed, real RELIANCE row (`isin=INE002A01018`, correct tick size), real NIFTY-family index rows. Every row's Upstox `instrument_key` (e.g. `"NSE_EQ|INE002A01018"`) stashed in `Instrument.exchange_token` — every other provider reads it back from there instead of reconstructing it (a deliberate convention that turned out to simplify market/historical/news/fundamentals considerably). Currency-derivative segments (`NCD_FO`/`BCD_FO`) excluded, same as Fyers' `NSE_CD` exclusion.
- [x] **10.4 Orders** (`brokerkit_upstox/order.py`, `mapper.py`, `errors.py`) — writes via `OrderApiV3` (`order_controller_v_3_api.py`), reads via `OrderApi.get_order_status` (single order — despite the misleading name, `get_order_details` on the same class returns order *history*, not a snapshot) and `OrderApi.get_order_book` (today's full book). Order status has **17 raw values** (verified against the official appendix, not guessed) collapsed into core's 6 — see `mapper.py`'s `_STATUS_MAP` comment for the reasoning behind each. Real quirk found only by testing SDK model construction: `ModifyOrderRequest` raises `ValueError` on `None` for `order_type`/`validity`/`price`/`trigger_price` (verified from the model's own property setters) — closer to Groww's "must resend everything" quirk than Fyers' "only changed fields," so `modify()` pre-fetches the current order to backfill anything not explicitly changed. `errors.py`'s `upstox_errors(default)` context manager follows Groww's shape (Upstox's SDK raises `ApiException`, unlike Fyers' error-dict style) — auth codes verified against Upstox's error-codes page (`UDAPI100016`/`UDAPI100050`/`UDAPI100067`/`UDAPI100073`, plus HTTP 401/403).
- [x] **10.5 Portfolio** (`brokerkit_upstox/portfolio.py`) — `holdings()`/`positions()` via `PortfolioApi`, both `api_version`-required (unlike the order-write path).
- [x] **10.6 Market Data + Historical** (`brokerkit_upstox/market.py`, `historical.py`) — implements `MarketDataProvider` (incl. `get_option_chain`) + `HistoricalDataProvider`. Batches up to 500 instrument keys/call (`MarketQuoteV3Api`). Upstox splits LTP/OHLC into two separate calls (no combined quote endpoint) — `get_quote()` calls both and combines; response dicts are keyed by an `"EXCHANGE_SEG:TRADINGSYMBOL"` string (verified from docs), not `instrument_key`, so results are re-keyed by each entry's own `instrument_token` field instead of parsing that key. Option chain (`OptionsApi.get_put_call_option_chain`) already groups by strike server-side (nicer than Fyers' flat array). **Bonus finding, live-verified**: `HistoryV3Api`'s three methods all set `auth_settings = []` in the generated SDK code — historical candles need **no token at all**, confirmed by a real unauthenticated live call (10 days of real RELIANCE daily candles, 2026-07-20).
- [x] **10.7 Streaming** (`brokerkit_upstox/streaming.py`) — wraps `MarketDataStreamerV3` (protobuf decoding handled internally by the SDK via `json_format.MessageToDict`, unlike Groww/Fyers' manual parsing). Real quirk verified from source: `connect()` spawns the websocket thread and returns immediately with no guarantee the socket is open yet (unlike Fyers'/Groww's blocking-enough `connect()`) — this adapter waits for the SDK's own `"open"` event (bridged via an `asyncio.Event`) before the first `subscribe()`, to avoid a race. LTP-only mode (`"ltpc"`) carries no volume/open_interest (`Tick.volume=0`, `open_interest=None`) — a real capability gap in that mode, not a bug.
- [x] **10.8 Fundamentals + News (new core interfaces)** — built as planned, most scrutiny of the whole phase.
  - `brokerkit-core/brokerkit/interfaces/fundamentals.py` / `news.py` — `FundamentalsProvider` (8 abstract methods, each takes `Instrument`) and `NewsProvider` (3 modes) ABCs.
  - `brokerkit-core/brokerkit/models/fundamentals.py` / `news.py` — `CompanyProfile`, `BalanceSheet`, `CashFlow`, `IncomeStatement` (typed single objects) + `FinancialLineItem`/`KeyRatio`/`CorporateAction`/`Competitor` (list-returning endpoints) + `NewsArticle`. `FinancialLineItem` (`label` + `history: list[FinancialPeriodValue]`) is a single shared model reused across cash_flow/income_statement/share_holdings' own entries *and* every `full_statement` breakdown — verified from the SDK's model files that these are all identically shaped (`{category|particular, history}`), so one model covers all of them instead of four near-duplicates.
  - **Placement decision held**: `fundamentals`/`news` are `UpstoxBroker`-only extra attributes, not on the shared `Broker` base — same YAGNI logic as Phase 4's dropped `Margin`.
  - `brokerkit_upstox/fundamentals.py`, `news.py` — real quirk found only by reading `fundamentals_api.py` directly: **7 of the 8 endpoints take `isin`, but `get_competitors` takes `instrument_key` instead** — a genuine Upstox API inconsistency, not a typo here. Also verified via the SDK's own `swagger_types` dicts: several response `data` fields (key_ratios/share_holdings/corporate_actions/competitors/news) are typed `object` (untyped) rather than a concrete model, while balance_sheet/cash_flow/income_statement/company_profile *are* concretely typed — the mapping code uses `.to_dict()` uniformly on every response rather than mixing attribute- and dict-style access, since `.to_dict()` normalizes both cases the same way. All 8 fundamentals endpoints built, none trimmed.
- [x] **10.9 Broker assembly** (`brokerkit_upstox/broker.py` — `UpstoxBroker`, `name = "upstox"`) — unlike Groww/Fyers, `create()` does **no eager network/login call** — constructing with only `analytics_token` must stay instant and silent (no forced browser popup for the fundamentals/news-only use case). The OAuth browser flow only fires lazily on first actual use of `orders`/`portfolio` (or of a data provider, in the analytics-token-absent fallback case). `close()` just closes streaming via the inherited `Broker.close()` — no background task to cancel (see 10.2).
- [x] **10.10 Verification** — done against a real account (separate sandbox at `/Users/aditya/Desktop/test/`, own `.venv`, `config.py`-based credentials — same convention as `fyers_test.py`). **Live-verified 2026-07-20**: `instruments.fetch_instruments()` (113,048 real rows), `historical.get_candles()` (real RELIANCE candles, no auth), and — via the Analytics Token, zero daily friction — market/fundamentals/news/streaming all confirmed working end-to-end. Orders deliberately not tested (static IP dashboard step, same as Groww/Fyers — explicit user call, "orders ko chod dena").

  **Two real bugs found and fixed from live data, not guessed**:
  - `get_corporate_actions`'s `expiry_date` comes back as `"05 Jun 2026"` (`"%d %b %Y"`), not ISO format like the docs' example implied — `fundamentals.py` now parses that format explicitly.
  - `get_key_ratios`' `company_value`/`sector_value` can be a percentage **string** like `"4.04%"`, not a plain number — `_decimal_or_none` now strips a trailing `%` before parsing, and raises a clear `f"could not parse {v!r}"` (showing the actual value) on anything else non-numeric, rather than a bare `ConversionSyntax` error.

  **Portfolio/orders (OAuth path) — real friction confirmed, deliberately deferred, not fixed**: Upstox's OAuth access_token has no documented headless refresh at all (confirmed once more in practice) — every regeneration needs a real browser login. `UpstoxOAuth` gained an `access_token` passthrough (plus `brokerkit_upstox.get_access_token(client_id, client_secret, redirect_uri)`, a standalone helper mirroring Fyers' `login_helper.py`, for minting one outside `create_broker()`) so an already-obtained token can be reused without re-triggering the browser flow every call — but the token itself still dies daily and must be re-minted by hand (or by revisiting the Selenium-automation-vs-unofficial-package fork, raised but **not yet decided**). Testing portfolio also hit a real `UDAPI100068` (client_id/redirect_uri mismatch — reusing Fyers' registered redirect_uri for the Upstox app was the likely cause, not yet confirmed fixed) and a local macOS gotcha (AirPlay Receiver squats on port 5000 by default, blocking the Flask capture server). **User's call after hitting this wall**: since Upstox's actual purpose in this stack is fundamentals+news (Analytics Token, zero friction) and not execution — same as Fyers — set orders/portfolio's OAuth friction aside as a known, accepted limitation rather than chasing it further right now.

  **Next resume point**: user is reviewing Upstox's docs directly to decide what additional scope to add (the diff step from the original plan — "test it, then diff what Upstox exposes vs what we built").

- [x] **10.11 Market Information (new core interface)** — first item off the user's post-testing docs review: all 12 endpoints under Upstox's "Market Information" category (`upstox.com/developer/api-documentation/market-information/`), which have no Groww/Fyers equivalent — F&O analytics (OI/change-in-OI/max-pain/PCR per strike), institutional flows (FII/DII activity), ranked screeners (futures/options/MTF smartlists), and market calendar/status (holidays/exchange-timings/market-status).
  - `brokerkit-core/brokerkit/interfaces/market_information.py` — `MarketInformationProvider` ABC, 12 abstract methods. Same placement call as Fundamentals/News: not on the shared `Broker` base, `UpstoxBroker`-only extra (`broker.market_information`).
  - `brokerkit-core/brokerkit/models/market_information.py` — one model per response shape (`OpenInterest`, `ChangeInOpenInterest`, `MaxPain`, `Pcr`, `InstitutionalActivity`, `Smartlist`/`MtfSmartlist` with their nested price/metric sub-models, `MarketHoliday`, `ExchangeTiming`, `MarketStatus`).
  - `brokerkit_upstox/market_information.py` — `UpstoxMarketInformation`, wraps `MarketApi` (9 methods) + `MarketHolidaysAndTimingsApi` (3 methods). Verified from SDK source (all 9 `MarketApi` methods return a fully-generic `AnalyticsResponse` with `data: object` — no swagger typing at all, unlike Fundamentals — so every response shape here was confirmed against real documented examples fetched live, not the SDK's types) that `get_market_holidays`/`get_exchange_timings` need **no token at all** (`auth_settings=[]`, same as historical candles); the rest need a token (Analytics Token confirmed sufficient — "Market Information" is explicitly listed as one of its covered categories).
  - **Real quirks found, not guessed**: (1) `get_oi`/`get_change_in_oi`/`get_max_pain`/`get_pcr` all use `"DD-MM-YYYY"` for their expiry field — **corrected 2026-07-21** after live-testing against a real account: the docs' own rendered example for `get_oi`/`get_change_in_oi` showed ISO `"YYYY-MM-DD"`, which turned out to be wrong/stale (real response: `"21-07-2026"`), not a real inconsistency between the four endpoints as first assumed from the docs alone — a reminder that a live response outranks a doc-rendered example, even one that looked authoritative. (2) The holiday response's real JSON key is `date`, but swagger-codegen renamed the Python attribute to `_date` (avoids shadowing the `datetime.date` import in that model file) — and since `.to_dict()` keys its output by the Python attribute name via `swagger_types`, not the true wire name via `attribute_map`, the mapper reads `d["_date"]`, confirmed by reading `holiday_data.py`'s source directly (both dicts) rather than assuming symmetry. (3) DII's `data_type` param has exactly one legal value (`"NSE_EQ|CASH"`) — hardcoded rather than exposed as a parameter with nothing to choose.
  - **Live-verified against a real account, 2026-07-21**: `get_oi`/`get_change_in_oi` (real NIFTY 50 data — trading_symbol for the plain index is just `"NIFTY"`, not `"NIFTY 50"`, `exchange_token` is `"NSE_INDEX|Nifty 50"`, another real quirk caught during test-script setup) — both work correctly after the expiry-format fix above.
  - **Live-verified 2026-07-20, unauthenticated**: `get_market_holidays()` (22 real holidays for the year) and `get_exchange_timings()` (real today's timings across NSE/BSE/MCX/NFO/BFO/CDS/BCD/NSCOM — confirming `Exchange`/`exchange` fields here deliberately stay plain `str`, not core's 3-value `Exchange` enum, since real data has far more segment codes than that). The 9 `MarketApi`-backed methods (OI/change-OI/max-pain/PCR/FII/DII/3 smartlists) are unit-tested against the real documented response JSON only so far — not yet live-called against a real account.

- [x] **10.12 Streaming switched from "ltpc" to "full" mode** (2026-07-20, same day — user's real use case: capture live ticks into a timeseries store and bucket them into 1-minute candles client-side, which needs volume). Verified from `MarketDataFeedV3.proto` that "ltpc" mode's `LTPC` message has no cumulative-volume field at all (only `ltq`, the single most-recent trade's size) — a real Upstox-specific gap, since Groww/Fyers' basic LTP feeds both carry real volume. Switched `UpstoxStreaming`'s `_MODE` to `"full"`; `_on_message` now unwraps the `fullFeed` `oneof` (`marketFF` for stocks/derivatives — has `vtt`/`oi` — vs `indexFF` for indices — neither field, correctly, since indices don't trade or carry OI), feeding `vtt`→`Tick.volume` and `oi`→`Tick.open_interest`. `upstox_to_tick()` gained `volume`/`open_interest` params accordingly. Trade-off accepted: "full" mode's per-connection subscription limit is 2000 instrument keys (Upstox's documented table) vs. "ltpc"'s 5000 — not a concern at the scale discussed. Unit-tested against fabricated feed payloads (with and without volume); not yet live-verified against a real dropped-in-market tick.

- [x] **10.13 `Tick.minute_ohlc` — server-computed live 1-minute candle** (2026-07-20, same day, direct follow-up to 10.12: user asked whether full OHLCV is obtainable from the websocket at all). Confirmed live example response for "full" mode shows `marketOHLC.ohlc` as a list with **both** a `"1d"` (daily) and an `"I1"` (1-minute) entry, each a real `{interval, open, high, low, close, vol, ts}` object, continuously updating — meaning the 1-minute candle doesn't need client-side tick aggregation at all, Upstox computes it server-side. Added `Tick.minute_ohlc: Ohlc | None = None` to **core** (`brokerkit-core/brokerkit/models/tick.py`) — deliberately a small, optional, backward-compatible cross-broker addition (unlike Fundamentals/News/MarketInformation's "Upstox-only extra" treatment) since it slots naturally into the existing universal `Tick` concept and costs Groww/Fyers nothing (stays `None`, verified their imports/construction still work unchanged). `UpstoxStreaming._on_message` extracts the `"I1"` entry from `source.get("marketOHLC")` — present on both `marketFF` and `indexFF` (unlike volume/OI), so `minute_ohlc` populates for index instruments too, not just stocks/derivatives. `upstox_to_tick()` builds it via the existing `upstox_to_ohlc()` helper (same open/high/low/close shape as the REST OHLC mapper). Unit-tested against the real documented example; not yet live-verified against a real market-hours tick.
  - **Cross-broker check, same day**: user asked if Fyers has the same capability for a pipeline consuming both brokers. Checked `fyers_apiv3`'s own `FyersWebsocket/map.json` directly — Fyers' `SymbolUpdate` feed's `open_price`/`high_price`/`low_price` are day-session level only (same fields as its REST quote), no per-minute/interval-tagged concept at all. `Tick.minute_ohlc` will only ever populate for Upstox; documented in this README and in a separate pipeline-requirements note (`/Users/aditya/Desktop/test/PIPELINE_REQUIREMENTS.md`, outside this repo — the user's actual consuming pipeline, not part of brokerkit).

- [x] **10.14 `ChargesProvider` (new core interface) — brokerage/tax calculator** (2026-07-20, same day, next docs-review item: `upstox.com/developer/api-documentation/get-brokerage`). `brokerkit_upstox/charges.py` — `UpstoxCharges`, wraps `ChargeApi.get_brokerage` (works off the Analytics Token — "Charges" is one of its covered categories, confirmed same as Fundamentals/News/MarketInformation). Same placement call as those three: new `brokerkit-core/brokerkit/interfaces/charges.py` (`ChargesProvider`, one method: `get_brokerage(instrument, quantity, product, transaction_type, price) -> BrokerageCharges`) and `models/charges.py`, not on the shared `Broker` base, `UpstoxBroker`-only extra (`broker.charges`). Response is fully typed in the SDK (`GetBrokerageResponse` → `BrokerageWrapperData` → `BrokerageData` → `BrokerageTaxes`/`OtherTaxes`/`DpPlan`, all concrete — unlike Market Information's generic blob), read via `.to_dict()` per the established pattern. Real field-name note: the SDK's declared model is `other_taxes` (`transaction`/`clearing`/`sebi_turnover`, no `ipft`); a doc-page-rendered example additionally showed camelCase-duplicate keys (`otherTaxes`/`dpPlan`) and an `ipft` sub-charge not present in the swagger type — matches this project's broader observation that several Upstox endpoints carry undeclared/duplicate legacy fields alongside the canonical ones; modeled here strictly against the SDK's own declared fields rather than the possibly-stale rendered example, so `ipft` is not currently captured (revisit if a live response actually includes it). `dp_plan` kept optional (delivery-sell-only per the docs example, not confirmed always-present/absent otherwise). `ChargeApi.post_margin` (margin requirement calculator) also exists in the same SDK class but was not requested and not built — out of scope for this pass. Unit-tested against the real documented example; not yet live-verified against a real account.
